# Kim 多级分镜提示词助手 - 开发完成记录

**项目版本**：v1.0  
**开发周期**：2026-03-04 ~ 持续更新  
**最后更新**：2026-03-05

---

## 📋 目录

1. [项目结构](#项目结构)
2. [已完成功能清单](#已完成功能清单)
3. [技术实现细节](#技术实现细节)
4. [UI/UX 优化](#uiux 优化)
5. [待开发功能](#待开发功能)

---

## 项目结构

```
f:\AI\AIProject\KimV4\
├── src/
│   ├── main.js              # 主进程入口（~66 行）
│   ├── preload.js           # 预加载脚本
│   ├── renderer.js          # 渲染进程（~2700 行）
│   ├── handlers/            # IPC 处理器
│   │   ├── project.js       # 项目管理（~260 行）
│   │   ├── api.js           # LLM API（~150 行）
│   │   └── template.js      # 模板管理（~140 行）
│   └── utils/               # 工具模块
│       └── menu.js          # 原生菜单 + 日志（~280 行）
├── assets/
│   └── default-templates.json
├── work/
│   ├── dev-log.md           # 开发日志
│   ├── update-log.md        # 更新日志
│   └── settings.json        # 开发配置
├── index.html               # 主界面
├── styles.css               # 样式文件（~990 行）
├── package.json
└── Kim 多级分镜提示词助手 - 完整优化版项目开发方案.md
```

### 模块化说明

| 模块 | 文件 | 行数 | 职责 |
|------|------|------|------|
| 主进程入口 | `main.js` | 66 | 窗口创建、模块初始化 |
| 项目 IPC | `handlers/project.js` | 260 | 项目 CRUD、文件监测 |
| API IPC | `handlers/api.js` | 150 | DeepSeek/豆包/千问 API |
| 模板 IPC | `handlers/template.js` | 140 | 模板配置、备份恢复 |
| 菜单工具 | `utils/menu.js` | 280 | 原生菜单、日志窗口 |
| 渲染进程 | `renderer.js` | 2700 | UI 逻辑、事件处理 |

---

## 已完成功能清单

### ✅ 阶段 1：基础功能（MVP）

#### 1.1 项目管理
- [x] 新建项目（手动/AI 生成两种模式）
- [x] 项目列表展示（名称、描述、状态）
- [x] 项目状态切换（草稿/进行中/已完成/已取消）
- [x] 项目删除（二次确认）
- [x] 项目文件夹打开
- [x] 项目数据持久化（project.json）

#### 1.2 片段（Shot）管理
- [x] 新建片段
- [x] 片段列表展示（名称、时长、画幅、镜头数）
- [x] 片段删除
- [x] 片段属性编辑（名称、时长、风格、情绪、音乐、音效、备注）
- [x] 片段属性自动保存（失焦触发，500ms 防抖）
- [x] 片段状态标签显示

#### 1.3 镜头（Scene）管理
- [x] 新建镜头
- [x] 镜头列表展示（编号、名称、类型、角度、时长）
- [x] 镜头删除
- [x] 镜头属性编辑（名称、类型、角度、运镜、时长、内容、台词、情绪、备注）
- [x] 镜头属性自动保存（失焦触发，500ms 防抖）
- [x] 镜头启用/禁用状态

#### 1.4 提示词功能
- [x] 单镜头提示词生成
- [x] 整片段提示词生成
- [x] 全项目提示词生成
- [x] 提示词预览（语法高亮）
- [x] 提示词复制到剪贴板
- [x] 提示词导出（TXT 格式）
- [x] 提示词清空

#### 1.5 模板库管理
- [x] 模板列表展示
- [x] 模板新建/编辑/删除
- [x] 模板激活/切换
- [x] 模板备份（导出 JSON）
- [x] 模板恢复（导入 JSON）
- [x] 模板文件夹打开

#### 1.6 AI 集成功能
- [x] DeepSeek API 集成
- [x] 豆包 API 集成
- [x] 通义千问 API 集成
- [x] API 连接测试
- [x] AI 生成提示词
- [x] AI 生成项目（基于提示词）
- [x] API Key 安全存储

#### 1.7 系统设置
- [x] 存储路径设置
- [x] API 配置（Key、模型名称）
- [x] 自动保存间隔设置
- [x] 主题切换（浅色/深色）

---

### ✅ 体验优化功能

#### 2.1 界面布局优化
- [x] 四栏横向平铺布局（项目/片段/镜头/提示词）
- [x] 分栏宽度拖拽调整
  - 项目栏：150-500px（初始 200px）
  - 片段栏：200-600px
  - 镜头栏：250-700px
  - 提示词栏：自动填充
- [x] 拖拽手柄视觉反馈（hover 高亮、拖拽线）
- [x] 状态标签右上角固定显示
- [x] 列表项右侧留白（避免遮挡）

#### 2.2 属性面板优化
- [x] 底部面板显隐控制（点击标题栏任意位置）
- [x] 属性/素材双栏布局（2/3 + 1/3）
- [x] 属性面板动态标题
  - 默认：「属性」
  - 选中片段：「{片段名} 属性」
  - 选中镜头：「{镜头名} 属性」
- [x] 属性表单滚动条优化（6px 细滚动条）
- [x] 失焦自动保存（500ms 防抖）
- [x] 保存后自动更新提示词

#### 2.3 原生菜单优化
- [x] 菜单结构调整
  - 文件（退出）
  - 编辑（标准编辑功能）
  - 视图（主题切换、缩放、全屏）
  - 系统（设置、模板库、日志、自定义选项）
- [x] 设置快捷键（Cmd/Ctrl+,）
- [x] 模板库快捷键（Cmd/Ctrl+T）

#### 2.4 日志功能
- [x] 独立日志窗口（1000x700px）
- [x] 日志分类
  - 开发日志（dev-log.md）
  - 更新日志（update-log.md）
- [x] Markdown 渲染（标题、列表、分隔线）
- [x] 深色主题样式
- [x] 日志文件自动初始化
- [x] 日志窗口无菜单栏（避免循环打开）

#### 2.5 交互细节优化
- [x] API Key 输入框可见性切换
- [x] 错误提示输入框下方显示（非居中弹窗）
- [x] 错误提示 2 秒自动消失
- [x] 更新通知右上角显示
- [x] 加载覆盖层显示
- [x] 状态标签点击切换（项目状态）
- [x] 列表项选中高亮（深灰色边框 + 浅灰色背景）

---

## 技术实现细节

### 3.1 自动保存机制

```javascript
// 失焦触发 + 500ms 防抖
let saveTimeout = null;
input.addEventListener('blur', () => {
  if (saveTimeout) clearTimeout(saveTimeout);
  saveTimeout = setTimeout(async () => {
    await saveProperties();
  }, 500);
});
```

### 3.2 分栏拖拽实现

```javascript
// CSS
.panel-resizer {
  position: absolute;
  top: 0;
  right: -1px;
  width: 5px;
  height: 100%;
  cursor: col-resize;
}

// JavaScript
resizer.addEventListener('mousedown', (e) => {
  isResizing = true;
  startX = e.pageX;
  startWidth = panel.offsetWidth;
  document.addEventListener('mousemove', handleMouseMove);
  document.addEventListener('mouseup', handleMouseUp);
});
```

### 3.3 状态标签定位

```css
/* 列表项 */
.list-item {
  padding: 8px 45px 8px 8px; /* 右侧留白 45px */
  position: relative;
}

/* 状态标签 */
.status-tag {
  position: absolute;
  top: 8px;
  right: 8px;
  z-index: 10;
}
```

### 3.4 模块化架构

```javascript
// main.js
const { initProjectIPC } = require('./handlers/project');
const { initApiIPC } = require('./handlers/api');
const { initTemplateIPC } = require('./handlers/template');
const { setMainMenu, initializeLogFiles } = require('./utils/menu');

// 初始化
app.whenReady().then(() => {
  initializeLogFiles();
  initializeDefaultTemplates();
  initProjectIPC(mainWindow);
  initApiIPC();
  initTemplateIPC();
  setMainMenu(mainWindow);
  createWindow();
});
```

### 3.5 日志窗口实现

```javascript
// 移除菜单栏
logWindow.removeMenu();

// Markdown 渲染
function renderMarkdown(md) {
  let html = md;
  html = html.replace(/^### (.+)$/gim, '<h3>$1</h3>');
  html = html.replace(/^## (.+)$/gim, '<h2>$1</h2>');
  html = html.replace(/^# (.+)$/gim, '<h1>$1</h1>');
  html = html.replace(/^---+$/gim, '<hr>');
  html = html.replace(/^- \[x\] (.+)$/gim, '<li>✓ $1</li>');
  html = html.replace(/^- \[ \] (.+)$/gim, '<li>○ $1</li>');
  return html;
}
```

---

## UI/UX 优化

### 4.1 视觉规范

| 元素 | 浅色主题 | 深色主题 |
|------|---------|---------|
| 背景色 | #ffffff | #1e1e1e |
| 文本色 | #333333 | #e0e0e0 |
| 边框色 | #e0e0e0 | #404040 |
| 悬停色 | #f0f0f0 | #2d2d2d |
| 选中色 | #e8e8e8 | #3d3d3d |

### 4.2 状态标签配色

| 状态 | 背景色 | 文字色 |
|------|--------|--------|
| 草稿 | #e3f2fd | #1976d2 |
| 进行中 | #fff3e0 | #f57c00 |
| 已完成 | #e8f5e9 | #388e3c |
| 已取消 | #ffebee | #d32f2f |

### 4.3 滚动条优化

```css
/* 细滚动条（6px） */
.property-form::-webkit-scrollbar,
.assets-list::-webkit-scrollbar {
  width: 6px;
}

/* 透明轨道 */
.property-form::-webkit-scrollbar-track {
  background: transparent;
}

/* 滚动块 */
.property-form::-webkit-scrollbar-thumb {
  background: #c1c1c1;
  border-radius: 3px;
}
```

---

## 待开发功能

### P0：高优先级

| 功能 | 预计工作量 | 说明 |
|------|-----------|------|
| 提示词实时生成优化 | 中 | 镜头/片段/项目三级联动 |
| 提示词类型切换 | 小 | 单镜头/整片段/全项目 |
| 提示词手动编辑覆盖 | 中 | 保存为自定义提示词 |

### P1：中优先级

| 功能 | 预计工作量 | 说明 |
|------|-----------|------|
| 拖拽排序 | 中 | 片段/镜头顺序调整 |
| 批量操作 | 中 | 批量删除/启用/禁用/导出 |
| 资源管理 | 大 | 图片/视频/音频上传预览 |

### P2：低优先级

| 功能 | 预计工作量 | 说明 |
|------|-----------|------|
| 深色主题完整适配 | 小 | 所有 UI 元素适配 |
| 响应式适配 | 中 | 小屏收缩/中屏缩略 |
| 日志自动记录 | 小 | 用户操作日志 |

---

## 版本历史

### v1.0（开发中）

**新增功能**：
- 项目/片段/镜头完整 CRUD
- AI 生成提示词（多平台支持）
- 模板库管理
- 系统菜单与日志功能
- 分栏拖拽调整
- 状态标签优化
- 失焦自动保存

**优化改进**：
- 主进程模块化重构
- 日志窗口独立显示
- 属性面板动态标题
- 滚动条视觉优化
- 错误提示位置优化

**Bug 修复**：
- API Key 测试连接读取问题
- 模板存储路径显示问题
- 错误提示焦点丢失问题

---

## 开发规范

### 代码规范
- 使用 ES6+ 语法（const/let、箭头函数、async/await）
- 函数命名：小驼峰（handleClick）
- 常量命名：大写下划线（MAX_WIDTH）
- 类名命名：大驼峰（WindowManager）

### 提交规范
```
<类型>(<范围>): <主题>

[可选 正文]

[可选 脚注]
```

**类型**：
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建/工具

---

**文档维护**：每次功能开发完成后，及时更新此文档。
