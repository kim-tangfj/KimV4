# 完整还原指南

由于修改内容过多（约 2000+ 行代码），建议按以下顺序手动还原：

## 1. 片段属性表单 (showShotProperties 函数)

**位置**: 约 2440 行

**修改内容**:
- 改为两列布局 (`shot-properties-grid shot-properties-2cols`)
- 添加新字段：角色、场景设定、图片参考、视频参考、音频参考、补充提示词
- 风格、情绪、配乐、音效字段改为下拉选择，带"+"添加按钮
- 添加 `setupOptionHintListeners(shot)` 调用
- 添加 `setupAddOptionButtons(shot)` 调用

## 2. 镜头属性表单 (showSceneProperties 函数)

**位置**: 约 2574 行

**修改内容**:
- 改为两列布局
- 景别、镜头角度、运镜改为下拉选择，带"+"添加按钮
- 添加新字段：分镜图片
- 添加 `setupSceneOptionHintListeners(scene)` 调用
- 添加 `setupAddOptionButtons(scene)` 调用

## 3. 保存函数

### saveShotProperties (约 2480 行)
添加新字段保存：
```javascript
const characters = document.getElementById('shotCharacters')?.value;
const sceneSetting = document.getElementById('shotSceneSetting')?.value;
const imageRef = document.getElementById('shotImageRef')?.value;
const videoRef = document.getElementById('shotVideoRef')?.value;
const audioRef = document.getElementById('shotAudioRef')?.value;
const customPrompt = document.getElementById('shotCustomPrompt')?.value;
```

### saveSceneProperties (约 2641 行)
添加新字段：
```javascript
const image = document.getElementById('sceneImage')?.value;
```

## 4. 自定义选项管理

### deleteCustomOption 函数
添加使用次数检查：
```javascript
const usageResult = await window.electronAPI.checkOptionUsage(optionId);
if (usageResult.usageCount > 0) {
  alert(`该选项已被使用 ${usageResult.usageCount} 次，无法删除。`);
  return;
}
```

### 新增函数
在适当位置添加：
- `setupOptionHintListeners(shot)` - 约 2550 行
- `setupAddOptionButtons(shot)` - 约 2560 行
- `showQuickAddOptionModal(group, field, shot)` - 约 2570 行
- `setupSceneOptionHintListeners(scene)` - 约 2700 行

## 5. CSS 样式 (styles.css)

添加以下样式：
- `.shot-properties-grid` - 多列布局
- `.shot-properties-2cols` - 两列布局
- `.property-column` - 列样式
- `.select-with-search` - 带搜索的下拉框
- `.usage-count-badge` - 使用次数徽章
- `.add-option-btn` - 添加按钮样式

## 6. 后端 API (src/handlers/options.js)

添加 IPC 接口：
- `options:incrementUsage` - 增加使用次数
- `options:checkUsage` - 检查使用情况

## 7. Preload API (src/preload.js)

添加：
```javascript
incrementOptionUsage: (optionId) => ipcRenderer.invoke('options:incrementUsage', optionId),
checkOptionUsage: (optionId) => ipcRenderer.invoke('options:checkUsage', optionId),
```

## 建议

由于修改复杂，建议：
1. 从之前工作的备份中恢复
2. 或联系之前开发的同事获取完整代码
3. 或分阶段重新实现这些功能
